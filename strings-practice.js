function makeabba(a, b) {
  return a + b + b + a;
}

function comboString(a, b) {
  if (a.length > b.length){
    return b + a + b;
  }
  return a + b + a;
}

function lastTwo(string) {
  if (str.length < 2) {
    return string;
  }
  return
}

console.log(makeabba("yaya", "blah"));
console.log(comboString("happy", "sad"));
